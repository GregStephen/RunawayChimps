diff --git a/docs/design-and-lore.md b/docs/design-and-lore.md
index da0668b..e2ea31f 100644
--- a/docs/design-and-lore.md
+++ b/docs/design-and-lore.md
@@ -658,4 +658,22 @@ For the current Level 1 completion lock, the separate panel represents the exist
 
-**Implemented regression protection / pending validation:** `Tools/validate_interaction_safety.py` is added to read-only Source Integrity. It checks these boundaries, includes eight intentionally broken source fixtures and fourteen rotated/scaled coordinate-math fixtures; these are not Unity or PhysX tests. Source results must be recorded against the tested commit. Unity 2022.3.55f1 import/compile, XRI 2.5.4 pickup/drop/re-grab and hand switching, reader enable/disable/teleport cases, ordinary floor/wall movement, complete two-card travel/retry, two-client Photon and Quest/headset comfort/performance remain pending.
+**Implemented regression protection / pending validation:** `Tools/validate_interaction_safety.py` is added to read-only Source Integrity. It checks these boundaries, includes eight intentionally broken source fixtures and fourteen rotated/scaled coordinate-math fixtures; these are not Unity or PhysX tests. Source results must be recorded against the tested commit. Unity 2022.3.55f1 import/compile, XRI 2.6.4 pickup/drop/re-grab and hand switching, reader enable/disable/teleport cases, ordinary floor/wall movement, complete two-card travel/retry, two-client Photon and Quest/headset comfort/performance remain pending.
 
 **Validated source result, 2026-09-15 UTC:** the reviewed patch applied to `aee33954d4fde1f249e1a8201b127562caa0ace9` passed the Unity 2022.3.55f1 declaration check, C# syntax/reference and five-scene checks, repository integrity, Level 1/keycard/PR15/security/threat contracts, all eight negative interaction fixtures and fourteen rotated/scaled math fixtures, Python compilation, merge-marker and whitespace checks in Apply interaction review run `34927689890`. This validates the patched source worktree, not Unity compilation or runtime. The final published commit and its read-only PR check are recorded on PR #23. Unity/XRI/Photon/headset validation remains pending.
+
+## September 15 UTC reusable-card review (PR #23)
+
+**Confirmed requirement:** Greg requested another thorough review because the physical card system will be reused across several levels. Review baseline is `17a3253921334733581fae9b361e796ed861a36e`; work stays on `feature/keycard-reader-light-feedback`. The previous sticky-hand/forcefield/unreliable-grab report remains failed runtime validation, and the historical one-card 1/2 result does not validate this revision.
+
+**Factual correction:** the installed XR Interaction Toolkit is **2.6.4** in both `Packages/manifest.json` and `Packages/packages-lock.json`. Prior review/PR wording saying 2.5.4 was incorrect and is superseded. Unity remains **2022.3.55f1** and Photon PUN is unchanged.
+
+**Implemented corrections:** all public physical-card acceptance now runs through one single-use transaction; consumption is committed before callbacks, preventing a listener or second public box entry from crediting the same card twice. An authored matching reader protects its lock in every level, not just the Level 1 travel lock. New Inspector-authored boxes default to requiring readers, with explicit compatibility for unbound legacy direct boxes. Reader authorization verifies its exact objective, the current explicit credential, local-held eligibility and actual root solid-collider overlap; accessory colliders and disabled Rigidbody collision cannot scan. Explicit null binding stays unbound, negative authored lookup is cached, rebinds invalidate old acknowledgement state, and re-enable/rebind rescans nearby solid candidates to recover sleeping-card overlaps.
+
+**Implemented pickup/recovery corrections:** new/legacy physical cards acquire held-collision safety; collider fitting excludes VFX and nested other cards; runtime-added cards refresh XRI's collider-to-interactable map. The two imported active cards serialized Instantaneous movement, contrary to the intended held-world collision behavior; `KeyCard` now normalizes to VelocityTracking while retaining lightweight continuous/interpolated physics. Release waits for XRI teardown, solid rig separation and both virtual hand query volumes; whole-object reactivation preserves protection. Fall recovery retains its temporary grab-disable ownership across deactivation, wakes loose cards to settle, and does not restore consumed cards. Panel subscription uses the actual bound source, ignores stale in-flight event arguments, respects explicit unbinding and never shows all-green progress merely because its lamp array is too small.
+
+**Implemented authoring/test support:** `CardSystemValidator` provides read-only loaded-scene and selected-prefab/hierarchy menus; `docs/keycard-system-reuse.md` records cross-level setup, supported lifecycle and acceptance gates. A .NET harness compiles actual `CardConsumptionState` production code rather than a Python translation. Unity Play Mode fixtures cover credential matrix, public API/cross-lock/reentrant consumption, binding, collider mapping, disable/re-enable, recovery, sleeping cards and physical settling; their engine execution remains pending. The source parser now includes those fixture/editor sources. Existing negative source fixtures and coordinate-math checks remain separate from engine tests.
+
+**Reuse boundary / unchanged design:** these remain personal, visit-local, consumed objective cards, not pooled reusable passes or shared Photon props. Mixed-credential quotas, persistent cross-level reward cards and non-consumed passes remain separate undecided/unimplemented design work. A multi-reader lock counts distinct accepted cards, not one quota per credential. Level 1 stays two Amber cards with the existing travel rules. The panel prefab still has four slots; larger requirements need more authored lamps. Fall recovery must be tuned below the lowest playable elevation of each new level. No scene geometry, global layer matrix, input binding, Crawler behavior or networking architecture is changed.
+
+**Pending validation:** run the new managed harness and complete source suite against the reviewed source, then run Unity import/compile, the new Play Mode fixture, read-only authoring validation, both existing Editor validators, real repeated card/hand/wall tests, full completion/travel/recovery, independent two-client Photon and Quest/headset checks. VelocityTracking responsiveness and wall behavior require hands-on validation. Source checks do not prove a perfect or runtime-ready system.
+
+<!-- reusable-card-validation-record -->
diff --git a/docs/keycard-system-reuse.md b/docs/keycard-system-reuse.md
new file mode 100644
index 0000000..a07439f
--- /dev/null
+++ b/docs/keycard-system-reuse.md
@@ -0,0 +1,50 @@
+# Reusable physical keycard system
+
+Review date: 2026-09-15 UTC. Implementation branch: `feature/keycard-reader-light-feedback`, PR #23. Review baseline: `17a3253921334733581fae9b361e796ed861a36e`. Unity **2022.3.55f1**, Photon PUN, XR Interaction Toolkit **2.6.4** (both manifest and lock file). The previous 2.5.4 references were documentation errors, not an installed-version change.
+
+## Supported contract
+
+These are **visit-local, personal, single-use physical objective cards**. Each client owns its scene copy; there is no network card transfer or shared card inventory. A card that has been held by the local rig can be presented to a matching, explicitly bound reader. Successful acceptance commits the card as consumed before notifying progress listeners, increments exactly one lock, disables acquisition, and destroys the card. A rejected attempt remains retryable. Leaving and reloading a level creates fresh cards/progress through the existing scene lifecycle.
+
+For Level 1, the two authored cards remain Amber Triangle, the two-card count and Level 2 travel guards remain unchanged, and the Crawler/capture/zone/networking systems are not rewritten. Reading a card changes the reader's immediate feedback; the separate panel owns persistent accepted progress. Prior 0/2 -> 1/2 runtime success is historical and narrow. The reported stuck hands/forcefield/repeated-pickup failures remain unclosed until retested on the reviewed code.
+
+A lock can have multiple explicitly bound readers. It currently counts **accepted distinct physical cards**, not a per-credential checklist. Two Amber submissions do not mean one Amber plus one Cyan. Per-credential quotas, reusable/non-consumed passes, cross-level saved reward cards, object pooling of consumed cards, shared multiplayer cards, and network-authoritative inventory are **not implemented or approved by this review**. Do not repurpose these visit-local instances for those contracts without a separate design and lifecycle change.
+
+## Author a card and reader in another level
+
+Create a gameplay root with `KeyCard`, `BoxCollider`, `Rigidbody`, `XRGrabInteractable`, `HeldItemCollisionMode` and an appropriate `RespawnToOriginalSpawn` configuration. Put the badge mesh beneath that root. Set a supported explicit credential: Amber Triangle, Cyan Three Bars, Red Circle or Violet Diamond. Do not combine the physical `KeyCard` with legacy `VRKeyCard`, which uses a collect-and-destroy inventory path. Do not place it under a PhotonView or a second card's Rigidbody.
+
+Startup fits the solid collider from renderer-local mesh bounds and normalizes the lightweight body plus **VelocityTracking** grab movement. Trails/particles and nested other cards are excluded from the fit. This supersedes the imported Instantaneous movement setting, which did not honor the intended physics-driven held prop. The acquisition collider is a separate child trigger, `Keycard_GrabAffordance`; it must not share a GameObject/layer-changing solid collider. The root solid collider alone is accepted for scanner geometry. Initial scale should be positive and nonzero; avoid sheared/nonuniformly scaled ancestor hierarchies. Run the runtime scale/rotation tests before using a new physical shape. A future interactor movement override must not silently switch these cards back to Instantaneous.
+
+Create the intended `KeyBox`, set `keysNeeded >= 1`, and enable **Require Matching Reader**. New Inspector-authored locks default to that setting. Assign the reader's `keyBox`, or deliberately nest exactly one box under its `readerRoot`. Give the reader one enabled trigger BoxCollider on its controller object, an explicit credential and valid status renderer/materials. A gameplay reader binding also latches its box into reader-only mode for that visit, so disabling or unbinding the reader cannot reopen direct insertion. Use the explicit flag for objectives whose readers start inactive. Unbound legacy boxes may intentionally retain direct insertion; their public `TryAddKey` now also consumes on success rather than providing a second count-only entry point.
+
+Place the separate progress panel, explicitly bind its box, and author one unique lamp renderer for each required card. The current reusable prefab contains **four slots**, previews two, and needs expansion for larger locks. Inadequate capacity logs a warning and stays amber rather than showing false completion. Lamp renderers must be children of the panel, not its root/ancestor or an unrelated object. Different scenes cannot share a live objective binding.
+
+Use `reader.BindObjective(source)` and `panel.Bind(source)` for dynamic setup. `Bind(null)` is an explicit unbind and will not silently rediscover an old nested/parent source. A reader attempts authored discovery once; late-created objectives must bind explicitly. Rebinding clears overlap/presence state and prevents an old acceptance callback from flashing the new binding. Re-enable/rebind performs a bounded-frequency nearby collider rescan after physics updates, including sleeping cards that may not produce a fresh trigger entry.
+
+For multi-storey maps, configure fall recovery **below the lowest playable route**, not merely three metres below a high pickup shelf. The existing spawn-relative threshold is suitable only where that vertical difference is out of bounds. Recovery now survives temporary object deactivation, wakes a loose body to settle under gravity, and never re-enables a consumed card. Do not disable only the held-collision safety component while an active prop is still held; release/deactivate the object as a whole. Whole-object reactivation retains separation protection.
+
+## Read-only authoring checks
+
+Stop Play Mode, then run:
+
+- **Tools > Runaway Chimps > Cards > Validate Loaded Scenes**: checks active objects in loaded scenes; inactive historical experiments are deliberately excluded.
+- **Tools > Runaway Chimps > Cards > Validate Selected Prefab or Hierarchy**: checks the selected hierarchy, including inactive children, before reusing it elsewhere.
+
+The validator checks component/credential setup, legacy inventory conflicts, personal-card Photon ownership mistakes, nested/extra collider bodies, positive scale, authored binding, reader triggers/materials, panel capacity and lamp wiring. It never saves a scene, changes a prefab, or applies SerializedObject changes. Template/unbound assets can intentionally report warnings/errors until authored for gameplay. Dynamic bindings must be independently verified in Play Mode. A successful authoring report is not a physics or multiplayer test.
+
+## Executable validation layers
+
+**Source checks:** existing Source Integrity retains seven trigger-ignoring Gorilla movement queries, local-corner fitting, local-rig isolation, reader overlap/credential/binding boundaries, and previous Level 1/travel contracts. The new reuse checks protect the consumption routing, rebind/reset behavior and XRI version assumptions. `validate_source.py --syntax` includes the new Unity fixture and editor-validator sources. Eight existing intentionally broken source fixtures and fourteen geometry-math fixtures remain. These cannot establish Unity compilation or PhysX behavior.
+
+**Managed production-state tests:** `dotnet run --project Tools/CardSystemHarness/CardSystemHarness.csproj --configuration Release` compiles the **actual** engine-independent production `CardConsumptionState.cs`. It exercises reference-identity locks, rejection/retry, reservation ownership, consumption-before-observer visibility, reentrancy and duplicate-credit rejection across 1,000 repeated sequences. It does not replace the engine: no XRI, Rigidbody, trigger, travel or Photon execution is claimed.
+
+**Unity Play Mode fixtures (implemented, pending execution):** open **Window > General > Test Runner > PlayMode**, filter `RunawayChimps.Tests.CardSystemPlayModeTests`, and run the fixture in an empty test context with the project packages installed. The test assembly uses TestAssemblies/UNITY_INCLUDE_TESTS and does not create a second runtime game rig. Because production remains in Assembly-CSharp, only the test's boundary uses reflection; no application assembly reorganization is required.
+
+The cases cover all 16 reader/card credential pairings, reader-required non-Level-1 locks, public direct-call rejection, unheld then eligible retry, cross-lock/cross-reader duplicate attempts, in-progress observer exceptions, explicit unbinding, stale and accessory overlap, disabled components/body collision, panel rebind and insufficient capacity, six 1x/2x rotation cases, actual programmatic XRI selection, live collider-map registration, recovery across deactivation, sleeping-card rediscovery, gravity settling and wake-after-recovery. Most authorization fixtures explicitly mark local-held state to isolate that boundary; the separate XRI event fixture tests real selection delivery. They do not simulate a physical controller grip, headset comfort or Photon clients.
+
+## Required acceptance before calling this reusable in production
+
+Run the Unity fixture and both existing project Editor validators; record the exact commit and failed case/log. Then complete repeated real-controller pickup/drop/immediate-re-grab, hand changes, flat/rotated/floor/wall cases, body/head proximity and held-card wall contact. Check that real walls still constrain movement, no invisible interaction volume acts as a wall, no pickup/release impulse moves the player, and the velocity-driven card remains responsive without objectionable lag or jitter. Identify the exact collider for any residual barrier; do not globally disable world collision.
+
+Exercise 0/2 -> 1/2 -> 2/2, exactly-once consumption, normal and failed-travel retry, capture drops, lower-floor recovery tuning, level leave/re-entry and pause/reconnect. Test two same-color locks separately, all wrong-card combinations, a lock with more than two lamps, disabled/rebound readers and objects, and a fresh scene using a non-Level-1 lock. Finally run two Photon clients in the same room and target Quest/headset tests: one player's cards/progress must never advance or disappear on another client's behalf. Green managed/source checks alone do not close these acceptance items.
