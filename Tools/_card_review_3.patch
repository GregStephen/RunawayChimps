diff --git a/Assets/Scripts/KeycardReaderLightController.cs b/Assets/Scripts/KeycardReaderLightController.cs
index e3f0abf..6c19b21 100644
--- a/Assets/Scripts/KeycardReaderLightController.cs
+++ b/Assets/Scripts/KeycardReaderLightController.cs
@@ -9,2 +9,3 @@ using UnityEngine;
 /// </summary>
+[RequireComponent(typeof(BoxCollider))]
 [DisallowMultipleComponent]
@@ -36,4 +37,11 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
+    private bool objectiveResolutionAttempted;
+    private int bindingRevision;
+    private int overlapReseedSteps;
+    private bool scannerWasActive;
+    private readonly Collider[] overlapScratch = new Collider[32];
+    public KeyBox Objective => keyBox;
+    public KeycardCredential ExpectedCredential => resolvedCredential;
     private BoxCollider scanVolume;
-    private bool ScannerActive => isActiveAndEnabled && scanVolume != null &&
+    private bool ScannerActive => this != null && isActiveAndEnabled && scanVolume != null &&
         scanVolume.enabled && scanVolume.isTrigger && scanVolume.gameObject.activeInHierarchy;
@@ -58,2 +66,6 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
+        if (resolvedCredential < KeycardCredential.AmberTriangle ||
+            resolvedCredential > KeycardCredential.VioletDiamond)
+            resolvedCredential = KeycardCredential.Auto;
+
         if (standbyMaterial == null && statusLedRenderer != null)
@@ -70,2 +82,37 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
+    private void OnEnable()
+    {
+        // A sleeping card may not generate another Stay after only the reader
+        // behaviour is re-enabled. Rebuild nearby candidates after physics catches up.
+        overlapReseedSteps = 2;
+    }
+
+    private void FixedUpdate()
+    {
+        if (!ScannerActive || overlapReseedSteps <= 0 || --overlapReseedSteps > 0)
+            return;
+
+        Vector3 scale = scanVolume.transform.lossyScale;
+        Vector3 halfExtents = Vector3.Scale(scanVolume.size * 0.5f,
+            new Vector3(Mathf.Abs(scale.x), Mathf.Abs(scale.y), Mathf.Abs(scale.z)));
+        Vector3 center = scanVolume.transform.TransformPoint(scanVolume.center);
+        Quaternion rotation = scanVolume.transform.rotation;
+        int count = Physics.OverlapBoxNonAlloc(center, halfExtents, overlapScratch,
+            rotation, ~0, QueryTriggerInteraction.Ignore);
+        // Do not silently lose a card in a crowded overlap. Allocation happens
+        // only when a one-time enable/rebind rescan fills the reusable buffer.
+        Collider[] candidates = count == overlapScratch.Length
+            ? Physics.OverlapBox(center, halfExtents, rotation, ~0, QueryTriggerInteraction.Ignore)
+            : overlapScratch;
+        if (candidates != overlapScratch)
+            count = candidates.Length;
+        for (int i = 0; i < count; i++)
+        {
+            Collider candidate = candidates[i];
+            if (ScannerActive && candidate != null && !acceptedColliders.Contains(candidate))
+                OnTriggerEnter(candidate);
+            candidates[i] = null;
+        }
+    }
+
     private void OnTriggerEnter(Collider other)
@@ -132,2 +179,3 @@ public sealed class KeycardReaderLightController : MonoBehaviour
         {
+            scannerWasActive = false;
             ClearOverlapState();
@@ -135,2 +183,5 @@ public sealed class KeycardReaderLightController : MonoBehaviour
         }
+        if (!scannerWasActive)
+            overlapReseedSteps = 2;
+        scannerWasActive = true;
 
@@ -145,2 +196,4 @@ public sealed class KeycardReaderLightController : MonoBehaviour
     {
+        scannerWasActive = false;
+        overlapReseedSteps = 0;
         ClearOverlapState();
@@ -163,5 +216,11 @@ public sealed class KeycardReaderLightController : MonoBehaviour
     {
+        bindingRevision++;
+        overlapReseedSteps = 2;
+        ClearOverlapState();
         keyBox = source;
+        objectiveResolutionAttempted = true; // Bind(null) is an explicit unbind, not a fallback request.
         warnedMissingProgressSource = false;
         warnedAmbiguousProgressSource = false;
+        if (keyBox != null && submitMatchingCardsToKeyBox)
+            keyBox.RegisterReader(this);
     }
@@ -183,3 +242,12 @@ public sealed class KeycardReaderLightController : MonoBehaviour
     {
-        if (keyBox != null || readerRoot == null)
+        if (objectiveResolutionAttempted)
+            return;
+        objectiveResolutionAttempted = true;
+        if (keyBox != null)
+        {
+            if (submitMatchingCardsToKeyBox)
+                keyBox.RegisterReader(this);
+            return;
+        }
+        if (readerRoot == null)
             return;
@@ -193,2 +261,4 @@ public sealed class KeycardReaderLightController : MonoBehaviour
             keyBox = nestedBoxes[0];
+            if (submitMatchingCardsToKeyBox)
+                keyBox.RegisterReader(this);
             return;
@@ -273,4 +343,12 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
-        if (!gameplayCard.TryInsertInto(keyBox))
-            return;
+        TrySubmitCard(gameplayCard);
+    }
+
+    /// <summary>Explicit callers get the same current-geometry/credential/binding checks as triggers.</summary>
+    public bool TrySubmitCard(KeyCard gameplayCard)
+    {
+        ResolveAuthoredKeyBox();
+        int submittedRevision = bindingRevision;
+        if (!CanSubmitCard(gameplayCard, keyBox) || !gameplayCard.TryInsertFromReader(keyBox, this))
+            return false;
 
@@ -278,5 +356,6 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
-        // A progress listener can disable the reader during acceptance.
-        if (!ScannerActive)
-            return;
+        // A progress listener can disable or rebind the reader during acceptance.
+        // Do not attach the old objective's acknowledgement to a new binding.
+        if (!ScannerActive || submittedRevision != bindingRevision)
+            return true;
 
@@ -288,2 +367,12 @@ public sealed class KeycardReaderLightController : MonoBehaviour
         SetAcceptedVisual(true);
+        return true;
+    }
+
+    internal bool CanSubmitCard(KeyCard card, KeyBox objective)
+    {
+        if (!ScannerActive || !submitMatchingCardsToKeyBox || objective == null || keyBox != objective ||
+            !objective.isActiveAndEnabled || card == null || !card.isActiveAndEnabled || card.IsInserted ||
+            !HasLiveOverlap(card.PhysicalCollider))
+            return false;
+        return TryResolveMatchingCard(card.PhysicalCollider, out KeyCard resolved) && resolved == card;
     }
@@ -312,3 +401,7 @@ public sealed class KeycardReaderLightController : MonoBehaviour
         KeyCard card = other.GetComponentInParent<KeyCard>();
-        if (card != null && (!card.isActiveAndEnabled || card.IsInserted))
+        if (card != null && (!card.isActiveAndEnabled || card.IsInserted || other != card.PhysicalCollider))
+            return false;
+
+        if ((other.attachedRigidbody != null && !other.attachedRigidbody.detectCollisions) ||
+            (scanVolume.attachedRigidbody != null && !scanVolume.attachedRigidbody.detectCollisions))
             return false;
@@ -405,4 +498,8 @@ public sealed class KeycardReaderLightController : MonoBehaviour
 
-        pendingGameplayCards.RemoveWhere(card =>
-            card == null || card.IsInserted || !HasMatchingOverlap(card));
+        pendingCardScratch.Clear();
+        foreach (KeyCard card in pendingGameplayCards)
+            if (card == null || card.IsInserted || !HasMatchingOverlap(card))
+                pendingCardScratch.Add(card);
+        foreach (KeyCard card in pendingCardScratch)
+            pendingGameplayCards.Remove(card);
     }
diff --git a/Assets/Scripts/Utils/RespawnToOriginalSpawn.cs b/Assets/Scripts/Utils/RespawnToOriginalSpawn.cs
index 0991c45..3d86fb7 100644
--- a/Assets/Scripts/Utils/RespawnToOriginalSpawn.cs
+++ b/Assets/Scripts/Utils/RespawnToOriginalSpawn.cs
@@ -18,2 +18,19 @@ public class RespawnToOriginalSpawn : MonoBehaviour
     Rigidbody rb;
+    KeyCard card;
+    bool restoreGrabPending;
+    Coroutine reenableRoutine;
+
+    void OnEnable()
+    {
+        if (restoreGrabPending)
+            ScheduleGrabRestore();
+    }
+
+    void OnDisable()
+    {
+        if (reenableRoutine != null)
+            StopCoroutine(reenableRoutine);
+        reenableRoutine = null;
+        // Keep ownership of our temporary disable across scene/prefab deactivation.
+    }
 
@@ -27,2 +44,3 @@ public class RespawnToOriginalSpawn : MonoBehaviour
         rb = GetComponent<Rigidbody>();
+        card = GetComponent<KeyCard>();
 
@@ -42,2 +60,4 @@ public class RespawnToOriginalSpawn : MonoBehaviour
     {
+        if (card != null && card.IsInserted)
+            return;
         // The original absolute KillY is a useful last-resort guard, but it is not enough
@@ -83,2 +103,4 @@ public class RespawnToOriginalSpawn : MonoBehaviour
     {
+        if (card != null && card.IsInserted)
+            return;
         if (verboseLogging)
@@ -91,2 +113,3 @@ public class RespawnToOriginalSpawn : MonoBehaviour
                 Debug.Log($"[Respawn] {name} was selected. Forcing release.");
+            restoreGrabPending = true;
             grab.enabled = false;
@@ -108,3 +131,4 @@ public class RespawnToOriginalSpawn : MonoBehaviour
             rb.rotation = spawnRot;
-            rb.Sleep();
+            // Let gravity settle the recovered card. Sleeping it here can strand
+            // a loose card above its support surface until another object touches it.
         }
@@ -114,5 +138,13 @@ public class RespawnToOriginalSpawn : MonoBehaviour
         Physics.SyncTransforms();
+        if (rb != null && !rb.isKinematic)
+            rb.WakeUp();
 
-        if (reenableGrab && isActiveAndEnabled)
-            StartCoroutine(ReenableGrabNextFrame());
+        if (restoreGrabPending)
+            ScheduleGrabRestore();
+    }
+
+    void ScheduleGrabRestore()
+    {
+        if (isActiveAndEnabled && reenableRoutine == null)
+            reenableRoutine = StartCoroutine(ReenableGrabNextFrame());
     }
@@ -123,3 +155,7 @@ public class RespawnToOriginalSpawn : MonoBehaviour
 
-        if (grab != null)
+        reenableRoutine = null;
+        if (!isActiveAndEnabled || !restoreGrabPending)
+            yield break;
+        restoreGrabPending = false;
+        if (grab != null && (card == null || !card.IsInserted))
             grab.enabled = true;
diff --git a/Assets/Tests.meta b/Assets/Tests.meta
new file mode 100644
index 0000000..eb16a51
--- /dev/null
+++ b/Assets/Tests.meta
@@ -0,0 +1,8 @@
+fileFormatVersion: 2
+guid: 7ccd01051ae942c189842d7ef32408e6
+folderAsset: yes
+DefaultImporter:
+  externalObjects: {}
+  userData:
+  assetBundleName:
+  assetBundleVariant:
diff --git a/Assets/Tests/CardSystem.meta b/Assets/Tests/CardSystem.meta
new file mode 100644
index 0000000..fb48b7d
--- /dev/null
+++ b/Assets/Tests/CardSystem.meta
@@ -0,0 +1,8 @@
+fileFormatVersion: 2
+guid: 2e5585f129934a25822a19fcbab6904f
+folderAsset: yes
+DefaultImporter:
+  externalObjects: {}
+  userData:
+  assetBundleName:
+  assetBundleVariant:
