name: One-time PR21 launch docs reconciliation

on:
  push:
    branches:
      - feature/launch-presentation-polish
    paths:
      - 'Tools/_sync_pr21_launch_docs.py'
      - '.github/workflows/pr21-launch-doc-sync.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/launch-presentation-polish
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Reconcile maintained PR21 launch docs
        run: python Tools/_sync_pr21_launch_docs.py
      - name: Commit reconciled docs
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- docs/design-and-lore.md docs/repository-improvement-plan.md; then
            echo 'Docs already reconciled.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/design-and-lore.md docs/repository-improvement-plan.md
          git commit -m 'docs: record restored distant launch terminal direction'
          git push origin HEAD:feature/launch-presentation-polish
